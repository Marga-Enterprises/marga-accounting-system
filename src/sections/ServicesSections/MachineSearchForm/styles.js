const styles = {
  searchForm: {
    mb: 3,
    display: 'flex',
    justifyContent: 'center',
  },

  searchStack: {
    marginTop: '1rem',
    width: '100%',
    maxWidth: 600,
  },

  searchInput: {
    flexGrow: 1,
    maxWidth: 400,
  },

  searchButton: {
    minWidth: 90,
  },
};

export default styles;
